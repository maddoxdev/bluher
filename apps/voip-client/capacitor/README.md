# VoIP Client (Capacitor)

Папка для мобильной оболочки (Android/iOS) поверх Angular.

## Рекомендуемый старт

```bash
npm i @capacitor/core @capacitor/cli
npx cap init Bluher com.bluher.app
```

Далее:

- `npx cap add android`
- `npx cap add ios`
